for i in [0: 5] {
    println("i = ", i);
}

// Calculate π
let pi = 0.0;
for k in [0: 10] {
    pi += 1 / (16.0 ** k) * (
        4.0 / (8*k + 1) -
        2.0 / (8*k + 4) -
        1.0 / (8*k + 5) -
        1.0 / (8*k + 6)
    );
}
println("pi = ", pi);
