
let global = 0;

fn increment()
{
	global = global + 1;
	return;
}

fn decrement()
{
	global = global - 1;
	return;
}

let dummy = increment();
dummy = increment();
dummy = increment();
dummy = decrement();

return global;

