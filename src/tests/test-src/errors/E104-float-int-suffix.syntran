// E104: float-int-suffix
// An integer type suffix ('i32 or 'i64) was used on a literal that's already
// shaped like a float (it has a decimal point or exponent)

let small = 7;
let medium = small * 2;

let big = 4.0'i32;
println(medium);
