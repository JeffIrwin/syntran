
let lparen    = char(40);
let rparen    = char(41);
let quote     = char(34);
let semicolon = char(59);
fn quine(s: str) {
	println(
		s + lparen + quote +
		s + quote + rparen + semicolon
	);
	return;
}
quine("
let lparen    = char(40);
let rparen    = char(41);
let quote     = char(34);
let semicolon = char(59);
fn quine(s: str) {
	println(
		s + lparen + quote +
		s + quote + rparen + semicolon
	);
	return;
}
quine");
